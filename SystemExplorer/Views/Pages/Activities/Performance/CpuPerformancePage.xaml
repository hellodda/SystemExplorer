<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SystemExplorer.Views.Pages.Activities.Performance.CpuPerformancePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SystemExplorer.Views.Pages.Activities.Performance"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:XamlToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="5" Padding="20 10 20 20">
            <Grid>
                <TextBlock FontSize="26" Text="CPU" HorizontalAlignment="Left"/>
                <TextBlock FontSize="18" Text="12th Gen Intel(R) Core(TM) i7-12700H" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>
            <Grid>
                <TextBlock FontSize="10" Text="% Utilization" HorizontalAlignment="Left"  Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock FontSize="10" Text="100%" HorizontalAlignment="Right"  Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </Grid>
            <SwapChainPanel Grid.Row="1" x:Name="GraphSwapChainPanel" HorizontalAlignment="Stretch" Height="380">
                <SwapChainPanel.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutSubItem Text="Change graph to">
                            <RadioMenuFlyoutItem Text="Overall utilization"/>
                            <RadioMenuFlyoutItem Text="Logical processors"/>
                            <RadioMenuFlyoutItem Text="NUMA Nodes"/>
                        </MenuFlyoutSubItem>
                        <ToggleMenuFlyoutItem Text="Show kernel times"/>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="Graph summary view"/>
                        <MenuFlyoutItem Text="Set point"/>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="Copy">
                            <MenuFlyoutItem.KeyboardAccelerators>
                                <KeyboardAccelerator Key="C" Modifiers="Control"/>
                            </MenuFlyoutItem.KeyboardAccelerators>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </SwapChainPanel.ContextFlyout>
            </SwapChainPanel>
            <Grid>
                <TextBlock FontSize="10" Text="4 minutes" HorizontalAlignment="Left"  Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock FontSize="10" Text="0" HorizontalAlignment="Right"  Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </Grid>
        </StackPanel>
        <StackPanel Grid.Row="1">
            
        </StackPanel>

        <!--<StackPanel Spacing="5" Padding="20">
           
            <controls:HeaderedContentControl Header="% Utilization">
                
            </controls:HeaderedContentControl>
        </StackPanel>-->
    </Grid>
</Page>
