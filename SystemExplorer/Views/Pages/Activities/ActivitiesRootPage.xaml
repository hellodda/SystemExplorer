<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SystemExplorer.Views.Pages.Activities.ActivitiesRootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SystemExplorer.Views.Pages.Activities"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tookit="using:XamlToolkit.WinUI"
    xmlns:essential="using:WinUI3Package"
    xmlns:helpers="using:SystemExplorer.Helpers"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <NavigationView
             x:Name="ActivitiesNavView"
             Grid.Row="0"
             Grid.Column="0"
             Grid.ColumnSpan="3"
             CompactPaneLength="70"
             IsBackButtonVisible="Collapsed"
             essential:NavigationViewHelper.AcrylicWorkaround="True"
             IsSettingsVisible="False"
             OpenPaneLength="200"
             PaneDisplayMode="Left"
             SelectionChanged="ActivitiesNavViewSelectionChanged">

            <NavigationView.Resources>
                <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent"/>

                <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent"/>

                <SolidColorBrush x:Key="NavigationViewDefaultPaneBackground" Color="Transparent"/>

                <SolidColorBrush x:Key="NavigationViewExpandedPaneBackground" Color="Transparent"/>

            </NavigationView.Resources>

            <NavigationView.MenuItems>

                <NavigationViewItem x:Name="ProcessesNavItem" Content="{x:Bind helpers:StringsHelper.Processes}"
                                    Tag="SystemExplorer.Views.Pages.Activities.ProcessesPage"
                                    Icon="{tookit:FontIcon Glyph=&#xECAA;}"/>

                <NavigationViewItem Content="Performance"
                                    IsEnabled="True"
                                    Tag="SystemExplorer.Views.Pages.Activities.Performance.PerformanceRootPage"
                                    Icon="{tookit:FontIcon Glyph=&#xE9D9;}"/>

                <NavigationViewItem Content="App history"
                                    IsEnabled="False"
                                    Icon="{tookit:FontIcon Glyph=&#xE81C;}"/>

                <NavigationViewItem Content="Users"
                                    IsEnabled="False"
                                    Icon="{tookit:FontIcon Glyph=&#xE716;}"/>

                <NavigationViewItem Content="Services"
                                    IsEnabled="False"
                                    Tag="SystemExplorer.Views.Pages.Activities.ServicesPage"
                                    Icon="{tookit:FontIcon Glyph=&#xEA86;}"/>

            </NavigationView.MenuItems>

            <Grid>
                <Frame x:Name="ActivitiesFrame" x:FieldModifier="Public" Loaded="ActivitiesFrameLoaded">
                    <Frame.ContentTransitions>
                        <TransitionCollection>
                            <NavigationThemeTransition />
                        </TransitionCollection>
                    </Frame.ContentTransitions>
                </Frame>
            </Grid>
        </NavigationView>
    </Grid>
</Page>
