<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SystemExplorer.Views.Pages.RootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SystemExplorer.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:XamlToolkit.WinUI.Controls"
    xmlns:flyouts="using:SystemExplorer.Views.Flyouts"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar
             x:Name="AppTitleBar"
             Title="System Explorer"
             Subtitle="InDev"
             Grid.Row="0"
             Height="45"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Top"
     
             IsTabStop="True">

            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/Square150x150Logo.png" />
            </TitleBar.IconSource>
            <TitleBar.RightHeader>
                <StackPanel Orientation="Horizontal" Spacing="5">
                    <Button Background="Transparent" BorderBrush="Transparent" HorizontalAlignment="Right">
                        <Button.Content>
                            <FontIcon FontSize="16" Glyph="&#xF4A5;" />
                        </Button.Content>
                    </Button>
                    <Button Background="Transparent" BorderBrush="Transparent" HorizontalAlignment="Right">
                        <Button.Content>
                            <FontIcon FontSize="16" Glyph="&#xE793;" />
                        </Button.Content>
                    </Button>
                    <Button ToolTipService.ToolTip="Center of action" Background="Transparent" BorderBrush="Transparent" HorizontalAlignment="Right">
                        <Button.Content>
                            <FontIcon Glyph="&#xE9A4;" />
                        </Button.Content>

                        <Button.Flyout>
                            <flyouts:ActionsCenterFlyout/>
                        </Button.Flyout>
                    </Button>
                </StackPanel>
            </TitleBar.RightHeader>
        </TitleBar>

        <NavigationView x:Name="NavView"
                 SelectionChanged="NavViewSelectionChanged"
                 OpenPaneLength="200"
                 IsSettingsVisible="True"
                 Grid.Row="1">
            <NavigationView.MenuItems>
                <NavigationViewItem
                    x:Uid="Nav.Root.Home"
                    Content="Home" Icon="Home"
                    Tag="SystemExplorer.HomePage"
                    />
                <NavigationViewItem
                    x:Uid="Nav.Root.System"
                    Content="System"
                    Tag="SystemExplorer.MainSystemInformationPage"
                    >
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE770;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem
                    x:Uid="Nav.Root.Monitoring"
                    Content="Monitoring"
                    Tag="SystemExplorer.SystemMonitoringRootPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xF120;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

            </NavigationView.MenuItems>
            <NavigationView.FooterMenuItems>
                <NavigationViewItem IsEnabled="False" SelectsOnInvoked="False" />
            </NavigationView.FooterMenuItems>

            <NavigationView.Content>
                <Frame x:Name="NavFrame"/>
            </NavigationView.Content>
        </NavigationView>
    </Grid>
</Page>
