<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SystemExplorer.Views.Pages.Properties.PropertiesRootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SystemExplorer.Views.Pages.Properties"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:items="using:SystemExplorer.Core.Data.Items"
    mc:Ignorable="d">

    <Grid>
        <NavigationView x:Name="NavView"
                        SelectionChanged="NavViewSelectionChanged"
                        MenuItemsSource="{x:Bind Properties}"
                        IsSettingsVisible="False"
                        IsBackButtonVisible="Collapsed"
                        PaneDisplayMode="Top"
                        IsPaneToggleButtonVisible="False">
            <NavigationView.MenuItemTemplate>
                <DataTemplate x:DataType="items:ObjectPairItem">
                    <TextBlock Text="{x:Bind First}" TextWrapping="Wrap"/>
                </DataTemplate>
            </NavigationView.MenuItemTemplate>

            <Frame x:Name="NavigationFrame"
                   x:FieldModifier="Public"/>
        </NavigationView>
    </Grid>
</Page>
