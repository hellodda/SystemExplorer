<?xml version="1.0" encoding="utf-8"?>
<local:BasePropertiesPage
    x:Class="SystemExplorer.Views.Pages.Properties.PropertiesGeneralPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SystemExplorer.Views.Pages.Properties"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="12" Spacing="4">
            <Grid
				Padding="12"
				Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
				BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
				BorderThickness="1"
				ColumnSpacing="8"
				CornerRadius="4"
				RowSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition
						Width="Auto"
						MinWidth="100"
						MaxWidth="140" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  File Icon  -->
                <Grid
					Grid.Row="0"
					Grid.Column="0"
					Width="56"
					Height="56"
					Margin="{StaticResource PropertyNameMargin}"
					HorizontalAlignment="Left"
					VerticalAlignment="Center"
					BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
					BorderThickness="1"
					CornerRadius="4">

                    <ImageIcon
                        x:Name="Icon"
						Width="48"
						Height="48"
						HorizontalAlignment="Center"
						VerticalAlignment="Center"
						AutomationProperties.AccessibilityView="Raw"
					/>
                </Grid>

                <StackPanel
					Grid.Row="0"
					Grid.Column="1"
					HorizontalAlignment="Stretch"
					VerticalAlignment="Center"
					Spacing="8">
                    <TextBlock Text="Sample Process Name"/>
                    <TextBlock Text="Sample Process Description"/>
                </StackPanel>

                <!--  Type  -->
                <TextBlock
					x:Name="ItemType"
					Grid.Row="1"
					Grid.Column="0"
					Margin="4,0,0,0"
					Text="Version:"
					FontWeight="SemiBold"
					Style="{StaticResource PropertyName}"
					 />

                <TextBlock
					x:Name="itemTypeValue"
					Grid.Row="1"
					Grid.Column="1"
					Text="1.0.0"
					Style="{StaticResource PropertyValueTextBlock}"
					 />
                <!--Text="{x:Bind ViewModel.ItemType, Mode=OneWay}"-->
                <!--  File System  -->
                <TextBlock
					x:Name="FileSystem"
					Grid.Row="2"
					Grid.Column="0"
					Margin="4,0,0,0"
					FontWeight="SemiBold"
					Style="{StaticResource PropertyName}"
					 />

                <TextBlock
					x:Name="DriveFileSystemValue"
					Grid.Row="2"
					Grid.Column="1"
					
					Style="{StaticResource PropertyValueTextBlock}"
					/>

            </Grid>
        </StackPanel>
    </ScrollViewer>
</local:BasePropertiesPage>
