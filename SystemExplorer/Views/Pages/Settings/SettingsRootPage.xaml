<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SystemExplorer.Views.Pages.Settings.SettingsRootPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SystemExplorer.Views.Pages.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Required"
    >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <NavigationView
        x:Name="SettingsNavView"
        Grid.Row="0"
        Grid.Column="0"
        Grid.ColumnSpan="3"
        CompactPaneLength="70"
        IsBackButtonVisible="Collapsed"
        IsSettingsVisible="False"
        OpenPaneLength="200"
        PaneDisplayMode="Left"
        SelectionChanged="SettingsNavViewSelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem
                    x:Name="GeneralNavItem"
                    Content="General"
                    Icon="Setting"
                    Tag="SystemExplorer.SettingsPage">

                    <NavigationViewItem.InfoBadge>
                        <InfoBadge />
                    </NavigationViewItem.InfoBadge>
                </NavigationViewItem>

                <NavigationViewItemSeparator />

                <NavigationViewItem Content="Appearance" Tag="SystemExplorer.Views.Pages.Settings.AppearancePage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE790;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Content="Advanced" Tag="SystemExplorer.Views.Pages.Settings.AdvancedPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xF1AD;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItemSeparator />

                <NavigationViewItem Content="About" Tag="SystemExplorer.Views.Pages.Settings.AboutPage">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE946;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <ScrollView>
                <Frame x:Name="SettingsFrame" x:FieldModifier="Public">
                    <Frame.ContentTransitions>
                        <TransitionCollection>
                            <NavigationThemeTransition />
                        </TransitionCollection>
                    </Frame.ContentTransitions>
                </Frame>
            </ScrollView>
        </NavigationView>
    </Grid>
</Page>
