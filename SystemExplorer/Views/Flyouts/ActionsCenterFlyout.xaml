<?xml version="1.0" encoding="utf-8"?>
<Flyout
    x:Class="SystemExplorer.Views.Flyouts.ActionsCenterFlyout"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SystemExplorer.Views.Flyouts"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:SystemExplorer.Models"
    xmlns:converters="using:SystemExplorer.Converters"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid.Resources>
            <converters:ActionStatusToBrushConverter x:Key="StatusToBrush"/>
        </Grid.Resources>

        <TextBlock Grid.Row="0"
                   Text="Actions Center"
                   FontSize="15"/>
        <Button HorizontalAlignment="Right"
                Grid.Row="0"
                Content="Clear"
                FontSize="14"
                Height="30"
                Width="100"/>

        <Border Grid.Row="1">
            <ListView>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:ActionItem">
                        <Border
                            Margin="4"
                            Padding="10"
                            CornerRadius="8"
                            Background="{Binding Status, Converter={StaticResource StatusToBrush}}">

                            <TextBlock
                                Text="{Binding Message}"
                                TextWrapping="Wrap"/>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Flyout>
